// 😍😒😘💕 As enum is best option for me as its predefined constant

enum DisplayType { average, total, min, max }

class BarchartData {
  final double value;
  final String label;
  BarchartData({
    required this.value,
    required this.label,
  });
}
